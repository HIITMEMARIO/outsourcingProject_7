review{
    id:uuid
    nickname:displayname,
    comment:comment,
    createdAt:sdfsdfjsdopi
hospital:병원id
}
불러올때
data.filter(item=>{
    return item.nickname === auth.currentuser.displayname
})

booking{
    id:uuid,
    nickname:displayname,
    hospital:병원id
    date:예약날짜
}

1. booking데이터를 가져온다.
2.booking데이터 중 nickname이 현재 로그인한 유저의 displayname과 같은 데이터를 필터해둔다.
3.선택한 병원의 place_name이 필터된 booking데이터 에 있으면 예약내역을 보여준다. 

마이페이지
1. booking데이터를 가져온다.
2.booking데이터 중 nickname이 현재 로그인한 유저의 displayname과 같은 데이터를 필터해서 map을 통해 뿌려준다.
3.수정 삭제 기능을 추가한다.



지금 맵에서 할 일
맵에 hospital과 같은 id를 가진 병원들을 검색 없이도 보여준다//


bookingData?.filter